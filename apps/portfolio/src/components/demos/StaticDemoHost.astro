---
import AlgorithmPlaygroundDemo from "@challenge/algorithm-playground-demo";

interface Props {
  slug: string;
}
const { slug } = Astro.props;
---

{
  slug === "algorithm-playground" ? <AlgorithmPlaygroundDemo /> : null
}
