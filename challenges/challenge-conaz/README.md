# Conaz Challenge

> Portfolio note: this challenge's maintained demo is now consolidated into
> the domain-based **Algorithm Playground**. The source below remains as
> immutable historical evidence.

History imported, sanitized, and verified.

## Portfolio presentation

The dependency-free JavaScript exercises remain available in the interactive
Astro demo under `../algorithm-playground/demo/`. The adaptation covers
character frequency, run-length encoding, and monthly member updates.

The historical implementation remains unchanged under `original/`. The
browser-safe adaptation lives under `../algorithm-playground/demo/`.

The former `challenge-conaz` and `javascript-data-exercises` routes redirect to
the canonical `algorithm-playground` entry.

Status: consolidated and automatically tested on 2026-09-10.
