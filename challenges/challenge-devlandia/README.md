# Devlandia Challenge

> Portfolio note: this challenge's maintained demo is now consolidated into
> the domain-based **Algorithm Playground**. The source below remains as
> immutable historical evidence.

History imported, sanitized, and verified.

## Portfolio presentation

The two original Ruby “Bot saves princess” solutions are represented by an
interactive Astro demo under `../algorithm-playground/demo/` with browser-safe
TypeScript logic. Visitors can edit a grid and calculate either the complete
path or the next move.

The historical Ruby implementations remain unchanged under `original/`. The
browser adaptation lives under `../algorithm-playground/demo/`.

The former `challenge-devlandia` and `grid-pathfinding` routes redirect to the
canonical `algorithm-playground` entry.

Status: consolidated and automatically tested on 2026-09-10.
